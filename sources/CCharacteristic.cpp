#include "CCharacteristic.h"

CCharacteristic::CCharacteristic(float maxHealth, float maxArmor, float speed, float maxLvl)
	: health(maxHealth), maxHealth(maxHealth), armor(maxArmor), maxArmor(maxArmor), speed(speed), maxLvl(maxLvl)
{}